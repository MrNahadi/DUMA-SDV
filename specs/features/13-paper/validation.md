# 13 · Paper: validation

## Automated
1. `npm run paper` exits 0; `paper/main.log` has no undefined references and no overfull boxes.
2. Every Feedback command passes (the sim fix in ADR 0017 keeps the reference tests green and adds a battery-limit test).

## Manual
1. Read the PDF: figures legible when printed, captions match the data, no brand names.
2. An engineer checks the architecture and results sections against the app.
